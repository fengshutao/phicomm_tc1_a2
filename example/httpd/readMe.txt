用例说明：
	httpd用例是用于对Web Server的本地数据处理。即改动网页内容后，可以直接在SDK应用层做修改，不用修改WebServer相关的底层代码。

测试方法：
	1.2M模块烧录该文件夹下的测试网页；
	2.电脑连接在模块的AP上；
	3.在浏览器中打开https://10.10.100.254；
	4.登录web管理页面，进入参数设置界面；
	4.该测试用例参数设置本地仅支持：心跳频率、采集频率、重启周期。

注意：
	应用层操作的关键字需要保证和网页中使用的一致；
